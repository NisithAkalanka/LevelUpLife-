<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Notification පෙන්වන්න අවසර ඉල්ලනවා -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- AlarmManager එකට device එක wake up කරන්න අවසර දෙනවා -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- add below existing permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LevelUpLife">

        <!-- ඔයාගේ ප්‍රධාන Activity එක -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Hydration Reminder එකේ Notification Receiver එක -->
        <receiver
            android:name=".util.NotificationReceiver"
            android:enabled="true" />

        <!-- Quest Progress Widget එකේ Receiver එක -->
        <receiver
            android:name=".ui.QuestProgressWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quest_widget_info" />
        </receiver>

    </application>

</manifest>